
extends layout

block append vars
  - bodyclass = 'article-detail'
  - bodycolour = page.metadata.colour

block prepend title
  | #{ page.title + " - "}

block header
  include author
  h1= page.title

block content
  div#content
    div.content-wrap
      article.article
        section.content!= typogr(page.html).typogrify()

block prepend footer
  div.nav
    a(href="/portfolio.html") « Portfolio
